schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
context: |
  aidd-training: servicio web para componer y enviar un email (Para/CC/CCO,
  asunto, cuerpo enriquecido con React Quill, adjunto unico <=10 MB) con envio
  real via SMTP (Nodemailer). Proyecto de formacion AIDD; sin persistencia.
  Stack: React + TypeScript + Tailwind (front), Node.js + Express + TypeScript
  (back), monorepo Turborepo + pnpm, despliegue local con Docker Compose.
  UI en espanol. Validacion y saneamiento de HTML siempre tambien en backend.
  Credenciales SMTP solo por variables de entorno.
  La fuente de verdad documental es docs/ (Markdown); docs/html/ son vistas
  generadas.

project_context:
  design_docs:            # diseno y definicion
    - docs/cliente-requisitos.md
    - docs/requisitos.md
    - docs/mapa-historias-usuario.md
    - docs/detalle-historias-usuario.md
    - docs/arquitectura-base-prototipo.md
    - docs/guia-estilos.md
    - docs/propuesta-arquitectura-base.md
    - docs/arquitectura-base.md
  delivery_docs:          # capa de entrega AIDD (solo las que existen)
    - docs/planificacion-proyecto.md
    - docs/sprint-plan.md
    - docs/plan-revision-hu.md

roadmap:
  generated_at: 2026-07-11
  context_budget: alto
  complexity: baja-media
  docs:
    roadmap: docs/roadmap.md
    prompts: docs/prompts-roadmap-native-ai.md
  phases:
    - id: 1
      name: "Foundation: monorepo y Docker"
      objective: "Walking skeleton: monorepo Turborepo+pnpm (web, api, shared) levantado con Docker Compose + Mailpit"
      context_risk: bajo
      change_hint: foundation
      sprint: "Sprint 1 · MVP (F0+F1) (id 35, 10-14 ago 2026)"
      hus: [HU-01, HU-02]
      effort_human: 3.0
      effort_ai: 0.4
    - id: 2
      name: "Backend de envío SMTP y logs"
      objective: "POST /api/send con Nodemailer (SMTP por env), logging pino sin contenido ni secretos"
      context_risk: bajo
      change_hint: api-envio-smtp
      sprint: "Sprint 1 · MVP (F0+F1) (id 35, 10-14 ago 2026)"
      hus: [HU-03, HU-04]
      effort_human: 3.5
      effort_ai: 0.9
    - id: 3
      name: "UI de composición"
      objective: "Formulario completo: chips Para/CC/CCO, asunto, cuerpo React Quill; esquema base en shared"
      context_risk: medio
      change_hint: ui-composicion
      sprint: "Sprint 1 · MVP (F0+F1) (id 35, 10-14 ago 2026)"
      hus: [HU-05, HU-06, HU-07, HU-09, HU-10]
      effort_human: 7.0
      effort_ai: 1.8
    - id: 4
      name: "Envío desde la UI y feedback"
      objective: "useSendEmail + apiClient + FeedbackBanner: envío directo y feedback de éxito/error (hito MVP F1)"
      context_risk: bajo
      change_hint: ui-envio-feedback
      sprint: "Sprint 1 · MVP (F0+F1) (id 35, 10-14 ago 2026)"
      hus: [HU-15, HU-16]
      effort_human: 2.0
      effort_ai: 0.5
    - id: 5
      name: "Validación de cliente"
      objective: "emailSchema (Zod) completo en shared como resolver RHF: obligatorios y formato de direcciones"
      context_risk: bajo
      change_hint: validacion-cliente
      sprint: "Sprint 2 · Endurecimiento (id 68, 17-21 ago 2026)"
      hus: [HU-08, HU-14]
      effort_human: 1.0
      effort_ai: 0.2
    - id: 6
      name: "Adjunto único con validaciones"
      objective: "Adjunto opcional extremo a extremo: reglas en shared, validación cliente de tipo/tamaño, Multer 10 MiB"
      context_risk: bajo
      change_hint: adjunto
      sprint: "Sprint 2 · Endurecimiento (id 68, 17-21 ago 2026)"
      hus: [HU-11, HU-12, HU-13]
      effort_human: 3.5
      effort_ai: 1.0
    - id: 7
      name: "Endurecimiento de servidor"
      objective: "Saneamiento HTML con whitelist y validación autoritativa en servidor (Zod + MIME real) (hito final)"
      context_risk: medio
      change_hint: endurecimiento-servidor
      sprint: "Sprint 2 · Endurecimiento (id 68, 17-21 ago 2026)"
      hus: [HU-17, HU-18]
      effort_human: 4.5
      effort_ai: 1.3

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

{
  "name": "aidd-training",
  "version": "0.0.0",
  "private": true,
  "description": "Servicio web para componer y enviar un email con envío real vía SMTP (proyecto de formación AIDD).",
  "packageManager": "pnpm@11.5.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
